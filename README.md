# BLcrypto — Криптография
Модуль для шифрования, расшифровки и хеширования данных в Go.
Подходит для микросервисов, совместим с Node.js.

## ✨ Возможности
- Шифрование AES-GCM (с защитой от подмены)
- Расшифровка AES-CBC (для данных с фронтенда)
- Хеширование по типам: email, телефон, имя, API-ключи и др.
- Все ключи передаются извне — безопасно и гибко

## 🚀 Установка
```bash
go get github.com/AndreyErr/crypto
```

## 💡 Пример
```go
12345678910
keys := crypto.KeyMap{
    "USER_EMAIL": []byte("32-байтный-секретный-ключ-здесь..."),
}
svc, _ := crypto.NewCryptoService(keys)

// Шифруем
enc, _ := svc.Encrypt("test@example.com", "USER_EMAIL")

// Хешируем
hash, _ := svc.HashData("Иван", "USER_NAME")
```
## ⚠️ Требования
- Все ключи — ровно 32 байта
- Ключи не хранятся в коде — загружайте из .env, Vault или секретов